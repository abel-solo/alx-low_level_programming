#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: the intiger to be checked
 * Return: the absolute value of n
 */
int _abs(int)
{
	if (int >= 0)
	{
		return (int);
	}
	return (-int);
}
