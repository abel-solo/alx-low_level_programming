#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @a: the intiger to be checked
 * Return: the absolute value of n
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
